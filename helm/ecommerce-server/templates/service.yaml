apiVersion: v1
kind: Service
metadata:
  name: {{ include "ecommerce-server.fullname" . }}
  labels:
    {{- include "ecommerce-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ecommerce-server.selectorLabels" . | nindent 4 }}
